class vehicule {

	int taille;
	int poids;
	boolean utilisable;
	
	boolean est_utilisable () {
	true;
	false;
	null;
	}

	void constructor (int taille, int poids){
		if (taille < 0)
			taille = 2;
		if (poids <= 0)
			poids = 100;
	}

	int get_taille () {
	this.taille;
	2;
	}

	void set_taille (int taille) {
		if (taille != 0){
			if (this.poids == 100)
				this.poids = this.poids++;
		}
		int tmp_size = taille;
		this.taille = tmp_size;
	}

	int augmenter_poids ( int count ) {
	int tmp;
	tmp = count;
	tmp = 5;
	tmp++;
	--tmp;
	++tmp;
	tmp--;
	tmp = (-1 * 10) + count * (1000 + 50) % 2;
	}

	string to_string () {
		String ad;
	}
}

class moto extends vehicule {
	int nb_roue;
	boolean side_car;
	
	
	a () {
		String n;
		Boolean a = true;
		n = (String)10;
		n = (boolean)a;
		
	}
	int calcul_vitesse_max ( int poids, int vitesse) {

	int pre_traitement;
	if (this.side_car){
		pre_traitement++;
		pre_traitement = pre_traitement / 10 - 30;
	}
	else
	{
		pre_traitement = 0;
	}
	}

	void constructor (int nb_roue, boolean side_car){
	vehicule vh1;
	vehicule vh2 = new vehicule();
	boolean check = this instanceof vehicule;
	boolean tmp_sidecar;
	tmp_sidecar = !side_car;
	
	}

	void deplacer ( int x, int y) {
		int i;
		for (i = ; i < 100; i++)
		{
			x++;
			y++;
		}
	}
}

public class toto { public static void maidsqlnscn(String ari []){;;;;;}}
